# Test Spec: test_gist_publisher_live

## Purpose
Live integration tests for GistPublisher with GitHub API.

## Change
Refactor: update import paths from `claude_code_analytics.streamlit_app.services` to `claude_code_analytics.services`. No behavior changes.
